import 'package:flutter/widgets.dart';

// Root navigator key to allow navigation from non-UI layers (e.g., interceptors)
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
